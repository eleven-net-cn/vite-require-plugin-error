# vite-require-plugin-error

Provide a minimal repository for: https://github.com/wangzongming/vite-plugin-require/issues/20

Terminal Error Logs:

```bash
[vite] Internal server error: p is not iterable
```

Even if there is not `require` syntax.
